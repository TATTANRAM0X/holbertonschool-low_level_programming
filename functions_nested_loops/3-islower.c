#include "main.h"

/**
* lowercase check
* @c: check_lowercase 
* Return: 0
*/

int _islower(int c)
{
char check_lowercase;
for (check_lowercase = 'a'; check_lowercase <= 'z'; check_lowercase++)
{
if (c == check_lowercasie)
return (1);
}
return (0);
}
