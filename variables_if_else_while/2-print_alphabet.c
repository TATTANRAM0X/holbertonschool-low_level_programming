#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints wheter a number is positive, negative or zero
 * Return: 0 Success
 */

int main(void)
{
char letra;
for (letra = 'a' ; letra <= 'z' ; letra++)
{
putchar(letra);
}
putchar('\n');
return (0);
}
